server:
  port: ${SERVER_PORT:8098}
  shutdown: graceful

spring:
  threads:
    virtual:
      enabled: true
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/voyage_db?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true}
    username: ${DB_USER:root}
    password: ${DB_PASS:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 10000
  servlet:
    multipart:
      max-file-size: ${FILE_MAX_SIZE:500MB}
      max-request-size: ${FILE_MAX_SIZE:500MB}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_id
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# =============================================
# Voyage 自定义配置
# =============================================
app:
  data-root: ${DATA_ROOT:D:/data/voyage}

performance:
  template-path: "${app.data-root}/uploads/performance/templates/"
  doc-path: "${app.data-root}/uploads/performance/docs/"

meta:
  max-file-lines: 2000
  storage:
    preview: "${app.data-root}/meta/preview/"
    formal: "${app.data-root}/meta/formal/"
  template-path: "${app.data-root}/meta/templates/"
  ref-file-path: "${app.data-root}/meta/ref-files/"

logging:
  level:
    com.voyage: debug
    root: info
